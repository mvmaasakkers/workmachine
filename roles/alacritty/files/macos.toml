# macOS-specific Alacritty settings (Intel and Apple Silicon)
# Imported by alacritty.toml

[font]
size = 13.0

[window]
option_as_alt = "Both"

[terminal]
shell = { program = "/bin/zsh", args = ["-l"] }
