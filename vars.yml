---
# Version management for development tools
# Override these in your own vars file or via command line

# Versions are on the top as they are most likely to be changed
lazygit_version: "0.64.1"
neovim_version: "0.12.4"
netbird_version: "0.77.1"
go_version: "1.27.0"
rust_version: "1.98.0"
nodejs_version: "24"
php_version: "8.5"
php_version_short: "8.5"
composer_version: "2"
python_version: "3.14"
nvm_version: "0.40.7"
codex_version: "0.149.0"
marksman_version: "2026-02-08"
eza_version: "0.23.5"
zsh_autosuggestions_version: "0.7.1"
zsh_syntax_highlighting_version: "0.8.0"
gh_version: "2.98.0"
terraform_version: "1.15.9"
terraform_checksums:
  linux_amd64: "76edd0b22d2f27d3d2e097cd793209646f719cf60f02ff3af626b07361137da1"
  linux_arm64: "0afa6c29f61ca5ea270e950e43e50ecf2418b598507bf580e8ae76e1e6699b19"
terraform_ls_version: "0.39.0"
terraform_ls_checksums:
  linux_amd64: "7750edc736845fd8c04ff0fc6332423c12d8275b358668c8c17e8aedc43ef971"
  linux_arm64: "62f32ea22cb78e5e5667ed638ad6e0fbde30ab59228d073c3c9bb249f89c7f5a"
packer_version: "1.16.0"
packer_checksums:
  linux_amd64: "5edcd14ab59b535040c512dbecd6ec9ef976a000b073c19d93e4c431c948581e"
  linux_arm64: "cf18f03460d92265d49b56befff333e80641d845822799eab04357c39f75b5d7"

# Platform detection
is_macos: "{{ ansible_os_family == 'Darwin' }}"
is_linux: "{{ ansible_os_family == 'Debian' }}"
is_arch: "{{ ansible_os_family == 'Archlinux' }}"
# Overridden by a pre_task in playbooks/setup.yml when /usr/share/omarchy
# exists on an Arch host (Omarchy preinstalls and manages many of our tools).
is_omarchy: false

# Architecture mapping
# Maps ansible_architecture to common architecture names used in downloads
arch_map:
  x86_64: "amd64"
  aarch64: "arm64"
  arm64: "arm64" # macOS reports as 'arm64' for Apple Silicon
  armv7l: "armv7"

# macOS-specific paths
homebrew_prefix_intel: "/usr/local"
homebrew_prefix_silicon: "/opt/homebrew"
homebrew_prefix: "{{ homebrew_prefix_silicon if ansible_machine == 'arm64' else homebrew_prefix_intel }}"

# Git commit signing (dedicated SSH key, generated by the git role)
git_signing_key: "{{ ansible_env.HOME }}/.ssh/id_ed25519_signing"

# Go settings
go_arch: "{{ arch_map[ansible_architecture] | default('amd64') }}"
go_install_dir: "/usr/local"

# Neovim
neovim_config_dir: "{{ ansible_env.HOME }}/.config/nvim"

# tmux
tmux_config_dir: "{{ ansible_env.HOME }}/.config/tmux"

# Zsh
zsh_config_dir: "{{ ansible_env.HOME }}/.config/zsh"

# Alacritty
alacritty_config_dir: "{{ ansible_env.HOME }}/.config/alacritty"

# Lazygit (OS-specific paths)
lazygit_config_dir_linux: "{{ ansible_env.HOME }}/.config/lazygit"
lazygit_config_dir_macos: "{{ ansible_env.HOME }}/Library/Application Support/lazygit"

# Font directories (OS-specific)
font_dir_linux: "{{ ansible_env.HOME }}/.local/share/fonts"
font_dir_macos: "{{ ansible_env.HOME }}/Library/Fonts"
font_dir: "{{ font_dir_macos if is_macos else font_dir_linux }}"

# Docker
docker_users:
  - "{{ ansible_user_id }}"
